export const EXTRACT_MESSAGES_PROMPT = `You are reading a chat page to extract new messages. Do NOT generate a response or take any action.

Extract any NEW messages visible on the page that are NOT already in the conversation history. Include rep messages AND system messages. Do NOT include messages we sent.`;
